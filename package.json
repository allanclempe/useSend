{
  "name": "usesend",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:editor": "pnpm --filter=@usesend/email-editor build",
    "build:web": "pnpm --filter=web build",
    "build:marketing": "pnpm --filter=marketing build",
    "build:smtp": "pnpm --filter=smtp-server build",
    "build:sdk": "pnpm --filter=usesend-js build",
    "build:web:local": "pnpm load-env -- pnpm --filter=web build",
    "dev": "pnpm load-env -- pnpm --filter=web dev",
    "test:web": "pnpm --filter=web test",
    "test:web:all": "pnpm test:infra:up && (pnpm --filter=web test:all; code=$?; pnpm test:infra:down; exit $code)",
    "test:web:all:raw": "pnpm --filter=web test:all",
    "test:web:unit": "pnpm --filter=web test:unit",
    "test:web:api": "pnpm --filter=web test:api",
    "test:web:integration": "pnpm --filter=web test:integration",
    "test:web:integration:full": "pnpm test:infra:up && (pnpm --filter=web test:integration:full; code=$?; pnpm test:infra:down; exit $code)",
    "test:infra:up": "docker compose -f docker/testing/compose.yml up -d --wait",
    "test:infra:down": "docker compose -f docker/testing/compose.yml down -v",
    "dev:worker": "pnpm --filter=web dev:worker",
    "dev:docs": "cd apps/docs && mintlify dev",
    "dev:marketing": "pnpm --filter=marketing dev",
    "dev:editor": "pnpm --filter=@usesend/email-editor dev",
    "lint": "pnpm -r --no-bail lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:generate": "pnpm db db:generate",
    "db:migrate": "pnpm db db:migrate",
    "db:push": "pnpm db db:push",
    "db:studio": "pnpm db db:studio",
    "db": "pnpm load-env -- pnpm --filter=web",
    "load-env": "dotenv -e .env",
    "d": "pnpm dx && pnpm dev",
    "dx": "pnpm i && pnpm dx:up && pnpm db:migrate",
    "dx:up": "docker compose --project-directory . -f docker/dev/compose.yml up -d",
    "dx:down": "docker compose --project-directory . -f docker/dev/compose.yml down"
  },
  "devDependencies": {
    "@usesend/eslint-config": "workspace:*",
    "@usesend/typescript-config": "workspace:*",
    "dotenv-cli": "^8.0.0",
    "mintlify": "^4.2.566",
    "prettier": "^3.5.3"
  },
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=22.13"
  }
}
